const init_state = {
  username: '',
  fullname: '',
  email: '',
  role: '',
  id: 0,
};

export default (state = init_state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
